简体中文 · [English](update.en.md)

### v1.2.1.0



#### ✨ 新增



- 【实验性】情绪模仿：按说话人的腔调、断句等模仿。

- 情绪/语气目录名不再限拼音，支持中文、英文、自造词、标点括号；匹配不区分大小写。

- 表情包按用途取短名收藏，换角色也能复用；收藏理由、用户画像规则由当前角色动态填充。

- 语音响度统一归一；参考音频电平统一处理。

- 心情值直接影响说话风格：低迷转烦躁，低正常转冷淡，回复压到 1~2 句。

- 表情包发送统一最大边长，默认 400，可关闭；动图不受影响。

- “网页预抓取”和“预抓取链接数量上限”加入工具调用配置页。

- 插件市场按目录上架：一个插件一个 `plugins/<分类>/<插件id>/` 目录（分类取自清单的 `category`，缺省归入「其他」），条目记在 `plugins/index.json`；一键发布写目录、更新索引，并把插件打成 zip 作为该版本 Release 的附件，下载量与收藏量取自 Release。

- 插件市场工具栏可按分类筛选，每个分类后面跟着插件数量，与搜索、排序叠加。

- 发布面板新增「下架」：删掉市场里的分类目录、索引条目，以及该插件的全部版本标签与 Release。

- 新增二级密码：在「WebUI 访问密码」下方设置，读取聊天记录、保存、导入导出、安装插件、删除、发布与下架等敏感操作需再输一次；解锁时长可配，0 = 每次都要输。



#### 🛠 修复



- 插件发布：同一版本发布成功后前端立刻显示「当前版本已发布过」，不再能重复推送；本地版本不高于已上架版本时后端直接拒绝，不能从 1.0 退回 0.1 制造版本混乱。

- 插件发布：「已上架 / 当前版本已发布过 / 下架按钮」改按当下的市场索引判定，不再吃市场页那份 30 分钟缓存 —— 别人在 GitHub 上删掉插件，回到面板立刻就不再显示已上架；本机发布 / 下架过的记录落盘保存，程序重启后仍拦得住重复推送、下架过的也不会再显示已上架；刚发布成功的插件立刻就能下架。

- 插件安装：新装的插件默认是停用状态，由用户自己去「插件」页启用（覆盖升级沿用原来的开关），不再一装上就跑起来。

- 插件发布面板：同一行不再同时出现「发布」和「下架」两个按钮，只给一个动作；发布 / 下架成功后按返回结果立刻切到新状态，不再等下一次查询（原来要慢好几秒）；推送期间「发布」按钮不可点，同一个版本不会被重复推。

- 表情包：LLM 自动收藏时按表情包根目录的命名风格归类，已有「撒娇」不会再新建一个 `sajiao`。

- 表情包：收藏时按图片内容查重，库里已有的图不再重复收藏一份；遗留的空目录不再当成分类，收藏不会落进看不见的目录。

- 表情包页：缩略图裂开、删不掉、中文标点文件名 404、WebP 裂图。

- 角色/会话：新会话误算成默认角色，换角色后用户画像冒出默认角色名；没填提示词的角色（如新建后未设置名称的空白人设）仍被注入默认角色的人设提示词。

- 情绪目录：不能新建中文标点/括号名，匹配区分大小写。

- 心情与审判：心情值在回复生成前被刷新；开关兼容层失效，字符串 false 被当开启。

- 问候/提醒：节日、生日、每日定时问候不承接历史、凭空开场；待办失败仍标完成；主动问候不写进会话历史。

- 自动收藏：分类命名受情绪影响；补充规则盖过硬性否决；分类不确定仍硬选。

- 自主学习：学到的含义被概括成抽象状态，有本义的词被按网络流行义理解；改为先按字面本义写，升级时旧提示词一并迁移。

- 状态保存：多处读取失败后用空数据覆盖磁盘；原子写残留 .tmp；并发保存互相覆盖。

- 用户数据位置：`config.json` 与 `data/` 改为固定存放在 `%LOCALAPPDATA%\Lovomo`，不再写在安装目录里 —— 换目录重装、覆盖升级都能接着用（老版本写在程序目录里的那份，首次启动会整份搬过去；搬不动就原地不动，绝不为了搬家丢数据）。卸载时会逐项询问是否删除配置、聊天记录、插件与本地记录，默认一律保留；「删除」现在会同时清理程序目录与用户目录两处，不再出现选了删除却什么都没删、或插件与发布记录永远清不掉的残留。

- 日志落点：探测程序目录是否可写时，若探测文件刚建好就被杀软、索引器或云同步盘占住句柄，删不掉会被误判成「目录不可写」，日志随之被静默改写到用户目录（排查时去程序目录找不到 `app.log`）。现在判据只看能否写入，清理失败不再改变结论。

- 插件、工具、知识库、词库、表情索引：原子写入、读取守卫、布尔字符串、默认值、空数据处理等问题修复。

- TTS/ASR：在线判定错误、超时失效、临时文件并发覆盖、TTS 耗时统计恒为 0。

- WebUI：配置保存抹掉未渲染字段、success:false 仍提示成功、插件发布 XSS、缓存/轮询异常；聊天记录按时间排序时仍按角色留出分组间隙。

- 其他：定时任务星期字符串匹配、插件市场版本匹配、YAML 空值失真、登录会话 0 被忽略、NapCat 令牌明文、统计 0 显示异常等。



#### ⚡ 优化



- 情绪目录扫描更耐错；默认情绪缺失启动告警；未识别情绪日志提示。

- 自定义情绪名可命中表情分类和画像提取。

- 旧配置自动迁移。

- 参考音频质量问题汇总提示，差音频不再进候选。

- 模型内容审核拦截时明确说明原因和出路。





### v1.2.0.0



#### ✨ 新增



**插件系统**



- 插件列表支持**置顶**：卡片右上角点「置顶」，置顶的插件排在「插件」页最前面（顺序记在插件状态文件里，重启后保留）。

- 插件可以自带 `webui.html`（固定文件名）：插件列表的卡片上多一个「打开webui」，点开是与功能页并列的独立页面，用来放状态面板、小工具这类与设置无关的界面。

- 插件功能页新增「查看README」按钮，随时能再看一遍插件自带的说明文档。

- 卸载插件时会同时询问是否清除插件配置（`data/settings.json`）与插件数据（整个 `data/` 目录），默认都保留；保留后重装同一个插件会接着用原来的设置与数据。

- 插件设置的下拉选项可以来自插件目录里的 JSON（清单写 `options_file`），运行时才知道的选项（如本机有哪些录音设备）也能做成下拉。

- 官方市场仓库可在配置页修改（`plugin_market_repo`），建议单独建一个市场仓库，别让插件分支堆在程序仓库里；程序仓库的目录分工是「`plugins/sources/<id>/` 放当前版本源码、`plugins/packages/` 放本地打包产物、上架内容全在分支上」。

- 一键发布自动打版本标签：`<分支名>-v<版本>`（如 `lovomo_plugin_meow-skin-v1.0.1`），同名标签不重复建，于是版本历史就是一串不可变标签，随时能按标签下载回滚。

- 版本没往上走就不给发布按钮：本地版本与已上架版本相同、本地还是 beta 而线上已是正式版、线上版本更高，这三种情况按钮会换成「当前版本已发布过（已上架 vX.Y.Z）」，改完清单里的 `version` 再发。

- 官方市场扫描的仓库、分支前缀、兜底索引与第三方市场地址都集中在「配置文件 → 插件系统」；分支列表按页拉全（GitHub 一次最多 100 条）。



**网络与搜索**



- GitHub 加速镜像（`github_mirrors`）：直连 GitHub 不通时（插件市场、版本信息、插件包下载）自动依次尝试配置里的镜像，配置页可一键「测速并排序」，用绿 / 黄 / 红标出每个镜像的延迟（绿 ≤600ms、黄 ≤1500ms、红 更慢或不可用），并按速度重排。

- 安全搜索加固：违禁词表大幅扩充（一般档约 185 条、严格档约 230 条），新增可编辑的「搜索过滤词表」和「搜索白名单词表」（`web_search_block_words`）（`web_search_allow_words`）自行追加。



**消息与发送**



- 会话白名单（`whitelist_ids`）：填写 QQ 号 / QQ 群号后，机器人只响应白名单内的会话，留空响应全部。



**防复读**



- 防复读判定系数可调：与角色历史台词 / 与用户原话的重合度阈值（`repeat_guard_self_threshold`、`repeat_guard_user_threshold`）可在 WebUI 调整。



**表情包**



- 发送方式四选一（`sticker_send_mode`）：关闭 / 随机 / 按情绪 / 按描述；选「按描述」时把候选表情的说明交给模型，由它挑出最贴合当前台词的一张。

- 识图收藏的表情包按模型给出的理由命名（同名自动加序号），后缀跟随图片真实格式。



**WebUI**



- 日志精简模式支持自定义隐藏片段（`webui_log_hide_patterns`）：按子串隐藏自动收藏、TTS 参数等噪音日志；勾选「显示完整日志」时全部照常输出。



#### 🛠 修复



**其他**



- 新装环境 WebUI 中 NapCat 连接配置整组不显示、无法编辑。

- 设置 WebUI 密码后，配置与聊天记录导出在浏览器报「需要密码」。

- 切换 WebUI 页面后再切回时回到页面顶部：现在各页面记住自己的滚动位置，配置页在表单刷新完成后还原。

- 打开聊天记录详情自动定位到最新一条；间隔超过 5 分钟的对话显示时间分隔（今天只显示时分，昨天 / 前天带相对日期，更早显示完整年月日）。

- 卸载程序不清理运行日志与临时文件：卸载时会询问是否删除 config.json 与 data（聊天记录等），覆盖安装升级的静默卸载路径不动用户数据；普通安装改为用户级目录，不再写入 Program Files。

- 程序安装到 Program Files 等只读目录时启动即崩（日志 / 配置写入被拒）：运行时文件自动改存到用户目录（`%LOCALAPPDATA%\Lovomo`），已有配置一并迁移；GBK 代码页终端下打印特殊符号不再中断程序。

- 桌面窗口中导出按钮点了没反应（WebView2 默认禁止页面下载）：配置与聊天记录导出改由系统「另存为」对话框落盘，浏览器访问保持直接下载。

- 导出的配置文件把解密后的明文密钥写了出去：导出一律保持 config.json 的 `enc:...` 加密占位符形式，任何明文密钥导出前重新加密。



#### ⚡ 优化



- 退出程序时隐藏一闪而过的CMD窗口。

- 主动消息/问候的参数可多选配置。





### v1.2.0.0-beta



#### ✨ 新增



**模型与 LLM**



- WebUI 模型可视化选择：LLM / 识图模型可点「选文件夹」扫描本地模型，或点「服务列表」直接拉取服务端的模型 ID，不再需要手抄模型标识符；TTS 模型目录、参考音频、表情包目录等路径字段均支持「浏览」一键选择。

- 换模型后自动卸载旧模型（`llm_auto_unload_old`，默认开启）：新模型首次调用成功时自动卸载不再使用的旧模型，避免多个模型常驻挤爆显存、拖慢回复。

- 文本清洗屏蔽词（`text_clean_blocklist`）：LLM 回复中出现的指定字符 / 词在发送前剔除，语音与文字同时生效。

- 搜索关键词由 LLM 提取（`search_query_llm_extract`）：预取搜索前由模型从用户消息里提取关键词、剔除口语与无关字符，失败或超时自动回退规则提取。

- 情绪判定规则引导（`emotion_guide_enabled`，默认开启）：提示词写明各情绪的使用时机，亲密 / 被撩 / 欲拒还迎场景必须优先「害羞」，「平静」只作最后选择；可追加自定义规则（`emotion_guide_extra`）。



**消息与发送**



- NapCat 发送超时自动重试（`send_retry`）：QQ 偶发「NTEvent Timeout」导致整句丢失时自动重发一次。

- 语音 / 文本分开发送时按 `。？！.!?` 强制分段后逐句请求 TTS，文字与语音一一对应。

- 回复审判心情按「会话 + 用户 + 角色」隔离：群聊里某人惹角色不开心，只降低对其本人的回复意愿。

- 群聊 @ 对象上下文注入，减少 LLM 混淆被 @ 的是谁。

- 用户画像支持按对话替换 / 删除旧条目（likes_remove、clear_* 等），防止旧信息残留。

- 待办提醒话术可选 LLM 生成或预设模板（`todo_remind_mode`），模型失败自动回退模板，绝不漏提醒；提醒语音可单独指定情绪（`todo_voice_emotion`）。



**语音与防复读**



- 识图模型可独立配置接口地址（`image_caption_base_url`，留空跟随 LLM 服务地址），云端视觉模型与本地聊天模型可以分开指向不同服务。

- 防复读拆成四个独立开关（默认全开）：按「什么时候查」分「流式首句发送前拦截」（`repeat_guard_streaming_check`）与「整段生成后校验重生成」（`repeat_guard_regen_check`），按「跟谁比」分「比对角色历史回复」（`repeat_guard_compare_self`）与「比对用户本条原话」（`repeat_guard_compare_user`），可自由组合。

- 合成时长的上限守卫（`tts_max_seconds_per_char`，默认 0.6 秒/字）：音频时长明显超出台词量级时判为异常并自动重试。



**系统与 WebUI**



- API 密钥改为真加密存储：密钥绑定本机硬件信息派生加密密钥（HMAC-SHA256 流加密 + 完整性校验），config.json 拷到别的电脑无法解出密钥；WebUI 与导出文件中只显示密钥头尾各 4 位和实际长度（如 `sk-a****6789（18位）`）



- 托盘常驻：关闭窗口最小化到托盘，托盘右键「打开 Lovomo / 重启 / 退出」。

- 聊天记录访问密码保护（`webui_password`）。

- 防刷屏限流（`anti_spam_*`）：同一会话短时间消息过多时自动忽略，不占用 LLM。

- 定期从 GitHub 检查更新，发现新版本时应用内弹窗提醒（`update_check_*`）。

- TTS 排查日志（`tts_debug_log`）：打印台词、情绪、参考音频等合成细节；字符替换表可在 WebUI 自定义（`tts_char_map`）。

- WebUI 左上角显示程序名称与当前版本号；侧边栏菜单整体下移，与标题分区更清晰。

- 程序更名为 Lovomo，数据库文件同步更名为 `lovomo.db`（首次启动自动沿用旧的 `ltvm.db`，统计与待办不会丢）。



#### 🛠 修复



**回复与情绪**



- 害羞的对话却用平静音色：模型输出的情绪名带中文 / 英文 / 变体（如「害羞」「shy」「haixiu。」）时被严格匹配打回——现在自动解析别名，解析不出才回退默认音色。

- 主动消息把模型的思考内容念了出来——请求、解析、流式、发送四层剥离思考内容。

- LLM 把自身回复或说话人标签误当作消息内容、把用户发的表情包说成「自己的」——标签规整 + 图片身份规则（`image_identity_guard_enabled`）。



**工具与搜索**



- 安全搜索漏放成人内容：只按少量关键词过滤，成人聚合站 / 黑料站 / 里番站（站名与域名）此前能穿过去，现在站名、域名与成人向词表都补齐了，并能按档位过滤。

- 发图片问主观问题却被拿去网上搜索：主观评价类问题不再触发搜索（提示词明确禁止 + 模型发起的此类搜索直接拦截），工具判断为 LLM 模式时同样生效。

- 搜索意图修改为识别忽略否定 / 质疑语境里的「搜」字。

- 不需要调用工具时 LLM 误调工具——消息不含触发词就不进工具流程，触发词可在 WebUI 配置。

- 调用搜索 / 网页工具报 `400 Bad Request`：发给服务的 tool_calls 消息形态不合规——发送前统一规整，报错附带服务端原始信息。

- 搜索关键词不准：整句口语直接当搜索词——剔除否定分句与请求语，只留搜索目标。

- 回复因相似度过高被打回重生成时，把同样的搜索再跑一遍（白等 30 秒以上）——预取搜索结果按关键词缓存，直接复用。



**TTS 与语音**



- 语音比文字「少一句 / 少一个词」：清洗白名单静默删符号、合成参数传反、中日句数对不齐时退化成整段合成——现在清洗只做等价替换并留日志、参数自愈纠正、句数不齐按字符权重并段，文字与语音逐段对应。

- 文字逐条发但语音粘成一整条（separate_send 下短句又被合并回去）。

- Windows 中文环境打印 ♪ 等符号抛 UnicodeEncodeError 打断合成。

- 一句台词尾部无限拖音：合成引擎偶发「同一个音节无限重复」（如一句 30 多字的台词拖成 27 秒的「に——」「呜——」）。现在长串拖音标记（`——`/`ー`）在送合成前压缩到 2 个，并对明显超出台词量级的音频作废重试，宁可这句只发文本也不发噪音。

- 排队中的下一条消息要等上一条语音播完才送进 LLM：发送侧的「按音频时长等待」原来连最后一条语音也照睡、且发生在会话锁内。现在改成「发下一条语音前才等上一条播完」，收尾那次等待彻底去掉，每条等待严格取自刚发出的那条语音本身的时长。

- 模型「服务列表」报 `400 Bad Request` 且地址从未填写过：服务地址被无条件拼上 `/v1/models`。现在地址已带端点后缀时原样使用，列模型时按候选地址逐个尝试，报错会列出实际请求过的地址与上游返回的原因，并提示应填写服务根地址。



**主动消息**



- 非静默时段也长期不发：计划时刻每次重算、闲置计时重启即丢、当日次数不落盘——现在计划只定一次、闲置计时按最后发言恢复、状态持久化到 `data/proactive_state.json`，并补齐诊断日志。

- 已删除的会话仍会继续收到主动消息：删除对话后闲置计时与当日计数还留在内存里，到点照发。现在发送前会校验会话记忆是否仍在，删除时同步清空该会话的主动消息状态。



**表情包**



- 纯风景 / 无文字图片被误收藏、GIF 被压成 PNG、收藏后发不出来——收藏需明确判定与具体理由、GIF/WebP 原样保留字节、自动同步通用池（`sticker_capture_any_pool`）。

- 识图自动收藏「没有任何反应」：判定通过却没落盘，而跳过路径全是静默返回，日志里看不到原因。现在收藏会直接复用识图时已读入的原始图片字节（不再第二次下载，QQ 图床短链失效也不会失败），最小间隔 / 每日上限 / 各开关未开 / 图片源不可用等每一种跳过都会写明原因。



**其他**



- RAG 在非 Ollama 后端下误判「嵌入模型未配置」。



**稳定性与数据安全**



- 会话记忆 / config.json / 心情 / 画像 / 事件 / 定时任务等全部 JSON 文件改为原子写入，进程被强杀不再产生半截文件；损坏时自动备份为 `.corrupt` 而不是静默清零。

- 修复对话后摘要任务把旧会话快照写回、覆盖刚保存的聊天记录的并发竞态；判定不回复或生成失败时用户消息也会正常落盘。

- config.json 损坏时先备份（`.corrupt`）再重建，不再直接用默认配置覆盖。

- 表情包上传增加图片格式白名单与 10MB 大小限制，回包加 nosniff，会话 Cookie 加 httponly，堵住存储型 XSS。

- 导出的配置文件不再包含明文 API 密钥（密钥保持 `enc:...` 加密占位符导出，重新导入自动还原）；保存角色等旁路写盘路径不再把密钥写成明文。

- 流式回复被截断时，抢救出的尾句现在会正常发送，而不是只写进历史。

- 识图请求中失效图片源不再导致后续图片 URL 与内容错位。

- 工具单回复调用配额改为按回复隔离，多群并发不再互相清零或互相占额；参数解析失败不再消耗次数。

- web_fetch 修复重定向校验失效（可被 302 跳转绕过内网防护）；DNS 校验移出事件循环不再卡顿全局；自定义命令工具输出按 UTF-8 容错解码；自定义 HTTP 工具遇到重定向返回明确错误而不是空结果。

- 待办提醒发送失败时保留待提醒状态，不再标记已完成；节日问候文案生成失败不再误标已发送。

- 定时任务增加重叠保护，回调慢于间隔时跳过本轮，不再堆积并发。

- 统计面板「今天」与逐日图表统一按本地零点分桶，不再把 0-8 点计入昨天。

- 修复合并语音时采样率/声道不一致导致的变速变调与合并失败、合成失败时临时文件泄漏、TTS 启动失败时并发消息各等 60 秒、路径配置为单个字符时启动崩溃。

- 「导出全部聊天记录」不再把 `webui_auth.json`（含 WebUI 令牌）一起打包；聊天记录的导入、导出、删除都只认会话记忆文件，不会误动同目录下的功能数据。

- 写接口增加跨站请求拦截（Origin/Referer 与 Host 不一致即拒绝），浏览器里的第三方页面无法再借 Cookie 直接操作本机控制台；命令行 / 脚本调用不受影响。

- 单个配置字段异常（如 `ref_audio_root` 为 `null`）不再把整份 config.json 判成损坏并重置；API 密钥解密失败时保留原密文，不再被写成空串。



#### ⚡ 优化



- 连接类故障（LLM / 嵌入服务不可达）报错附带目标地址与排查建议，启动时自动探测。

- WebUI「保存配置」与默认配置合并，界面未渲染的字段不再被丢掉。

- 表情包收藏判定搭车识图调用（零额外请求）；心情统计可展开多会话记录；工具测试支持自定义 JSON 参数；JSON 编辑区可拉伸。

- 删除会话时同步清理对应角色的心情记录。



